= form_for @policy , html: { id: 'policy_form', multipart: true } do |f| 
  = f.fields_for :CategoryCompany do |p|
    = p.select :company_id, options_for_select(@company.map{ |c| [c.name, c.name]},selected: @company_id), {prompt: ApplicationHelper::DEFAULT_SELECT_OPTION}, {class: "form-control select"}      